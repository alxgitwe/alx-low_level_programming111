#include "main.h"
#include <stdio.h>
/**
 * main -  test that the function positive_or_negative()
 * Return: return the value "0"
 */

int main(void)
{
	int i = 0;

	positive_or_negative(i);

Return(0);

}
