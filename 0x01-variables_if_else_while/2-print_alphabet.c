#include <stdio.h>
/**
*main - prints the alphabet in lowercase, followed by a new line
*Return: give a return 0 success
*/
int main(void)
{
	char alphabet;
	for (alphabet = 'a'; c <= 'z'; ++alphabet)
		putchar("%c \n", c);
	return (0);
}
